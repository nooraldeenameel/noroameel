# URL Downloader Web UI

واجهة بسيطة لإضافة رابط ملف وتحميله مباشرة.

## التشغيل

```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python app.py
```

ثم افتح:

- `http://localhost:8000`
